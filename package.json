{
  "type": "module",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.3.0",
    "@types/chrome": "^0.1.37",
    "tslib": "^2.8.1"
  },
  "scripts": {
    "dev": "rollup -c --watch",
    "build": "rollup -c --minify"
  }
}
